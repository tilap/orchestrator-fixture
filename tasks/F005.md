---
id: F005
dependencies: []
scope:
  - src/format/**
validation: [lint, test, build]
---

## Acceptance
- Numeric columns are right-aligned, text columns stay left-aligned
- A column is numeric only when every one of its cells is
- The separator rule keeps matching the column widths
- Existing callers of table keep working unchanged

## Constraints
- Do not compute anything about the values beyond how wide they print
- Do not import from src/core
