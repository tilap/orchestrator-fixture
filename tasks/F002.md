---
id: F002
dependencies: []
scope:
  - src/core/**
  - tests/core.test.mjs
validation: [lint, test, build]
---

## Acceptance
- Minimum and maximum of a column are available alongside sum and mean
- Both return null on an empty column, never NaN and never Infinity
- Both refuse a non-number the same way sum already does
- The existing behaviour of sum and mean is unchanged

## Constraints
- Do not change the signature of sum or mean
- Do not import anything from src/format or src/parse — core knows nothing about them
