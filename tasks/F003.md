---
id: F003
dependencies: [F001, F002]
scope:
  - src/report/**
  - tests/report/**
validation: [lint, test, build]
---

## Acceptance
- Parsed rows become a rendered summary table as a returned string
- Every numeric column reports count, sum, mean, minimum and maximum
- A non-numeric column is listed with its count only, never with a fabricated total
- The function returns the string and prints nothing

## Constraints
- Do not print, do not exit, do not read the filesystem
- Do not reimplement arithmetic that src/core already provides
